# ── Supabase (only key needed) ────────────────────────────
SUPABASE_URL=https://your-project-ref.supabase.co   # Dashboard → Settings → API → Project URL
SUPABASE_SERVICE_KEY=           # Dashboard → Settings → API → service_role key

